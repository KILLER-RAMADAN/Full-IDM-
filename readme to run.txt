install all libraries to run python script

by following command in your terminal=>(pip install <name of the laibrary>) <important>

to install full program without problems run this command (git clone + the link of repo) <important>

make sure to install all libraries to provide any problems...

################################################
import tkinter as tk
import sys
from tkinter import ttk, messagebox ,filedialog
from tkinter.ttk import *
import webbrowser
import threading
import yt_dlp as youtube_dl == version(2023.6.21)
import re
import os
import yt_dlp
import webbrowser
import os
import tkinter.font as font
################################################



